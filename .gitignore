# Claude Code (keep skills in repo, ignore the rest)
.claude/*
!.claude/skills/

# zapret2 upstream (separate repo, installed at /opt/zapret2)
zapret2/

# Flowseal lists and binaries (managed separately)
zapret-latest/
